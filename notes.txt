## Things to add/fix
- Instanced objects do normal matrix calculation on CPU and upload as attribute
- Manually calculate TBN matrix to support normals for things not loaded from Assimp
- Reduce loading time when starting 
	- cubemaps: reduce resolution, lazy loading, multithreading? 
	- figure out a way to save tangent/bitangent so it doesn't have to be calculated everytime
- Get MSAA working
- Setup debug output
- Figure out if using correct versions of libraries for debug vs release mode
- Point shadows
- improve directional shadows
	- dynamically update shadow map based on camera frustum
	- cascaded shadow maps
	- better PCF
	- fix peter panning
- fix city model textures (https://sketchfab.com/3d-models/city-scene-c00b3b8c71594890a2a3c46c20a30b5c?)
- better way to name/id objects in the scene
- automatic exposure?
- bloom w/ gausian blurr
- update/reorganize GUI code (maybe some sort of seperate data and settings structure)?
- make instanced objects work with other materials
- instanced objects w deferrred rendering
- better visualization shaders for debugging gBuffer buffers
- debug labels for renderdoc
- light volumes for large amount of lights in deferred rendering
- fix color banding issue for dark textures in deferred path
- calculate fragPosDirLightSpace per vertex instead of per fragment for deferredPhongFS?
- change random number gen (for SSAO)?
- remove ambient componenent from point lights
- gBufferVS.glsl/phongVS.glsl, reorthogonalize using gram schmidt process. Instead of using bitangent from assimp, figure out handedness first 
  (to see order of cross product) then calculate bitangent after gram schmidth using N and T
- organize GUI, add light settings for ambient light, directional light (maybe make them tied to the scene)